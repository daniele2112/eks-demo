apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: podinfo
  namespace: dev
  labels:
    k8s-app: podinfo
    app: podinfo
spec:
  jobLabel: podinfo
  selector:
    matchLabels:
      app: podinfo
  endpoints:
  - port: http-metrics
    path: /metrics
    interval: 15s